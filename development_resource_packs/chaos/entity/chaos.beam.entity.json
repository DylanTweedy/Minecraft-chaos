{
  "format_version": "1.21.0",
  "minecraft:client_entity": {
    "description": {
      "identifier": "chaos:beam",
      "materials": { "default": "entity_beam_additive" },
      "textures": { "default": "textures/blocks/chaos_beam" },
      "geometry": { "default": "geometry.chaos.beam.entity" },

      "animations": {
        "beam_scale": "animation.chaos.beam.scale"
      },

      "scripts": {
        "pre_animation": [
          "variable.beam_len = query.property('chaos:beam_len');"
        ],
        "animate": [ "beam_scale" ]
      },

      "render_controllers": [ "controller.render.chaos_beam" ]
    }
  }
}
